// Placeholder to confirm vitest infrastructure is wired correctly.
// Real tests live in tests/server/ and tests/client/.
import { describe, it, expect } from 'vitest';

describe('test infrastructure', () => {
  it('vitest is configured', () => {
    expect(true).toBe(true);
  });
});
